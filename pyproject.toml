[project]
name = "locness-ph"
version = "0.1.0"
description = "Read data from MBARI mFET pH sensor. Stores data in a csv file and sqlite3 DB."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy==2.0.1",
    "pyserial==3.5",
    "pyyaml==6.0",
]
